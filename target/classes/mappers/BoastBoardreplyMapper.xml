<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.puppy.boastboard.mapper.IBoastBoardReplyMapper">

   <resultMap type="com.spring.puppy.command.BoastBoardReplyVO" id="replyMap">
      <result property="replyId" column="reply_id" />
      <result property="replyPw" column="reply_pw" />
      <result property="replyDate" column="reply_date" />
      <result property="updateDate" column="update_date" />
   </resultMap>

   <insert id="replyRegist">
      INSERT INTO boastreply(rno, bbno, reply)
      VALUES(boastreply_seq.NEXTVAL, #{bbno}, #{reply})
   </insert>
   
   <select id="getList" resultMap="replyMap">
      SELECT * FROM   
         (
         SELECT ROWNUM AS rn, tbl.* FROM   
            (
            SELECT * FROM boastreply
            WHERE bbno = #{bbno}
            ORDER BY rno DESC
            ) tbl
         )
      <![CDATA[
      WHERE rn > (#{paging.pageNum} - 1) * #{paging.countPerPage}
      AND rn <= #{paging.pageNum} * #{paging.countPerPage}
      ]]>
   </select>
   
   <!--  
   <select id="getList" resultMap="replyMap">
      SELECT * FROM freereply
      WHERE bno = #{bno}
      ORDER BY rno DESC
   </select>
   -->
   
   <select id="getTotal" resultType="int">
      SELECT COUNT(*) FROM boastreply
      WHERE bbno = #{bbno}
   </select>
   
   <select id="pwCheck" resultType="int">
      SELECT COUNT(*) FROM boastreply
      WHERE rno = #{rno} AND reply_pw = #{replyPw}
   </select>
   
   <update id="update">
      UPDATE boastreply
      SET reply = #{reply}, update_date = sysdate
      WHERE rno = #{rno}
   </update>
   
   <delete id="delete">
      DELETE FROM boastreply
      WHERE rno = #{rno}
   </delete>
   

</mapper>